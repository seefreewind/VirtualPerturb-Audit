# Next Actions

1. Carry the L2/L3 retrieval-collapse pattern into the manuscript: completed GEARS rows show stable delta-Pearson (≈0.98) with top-1 retrieval dropping from 0.20 (L1) to 0.075 (L2) and 0.08 (L3), the main shortcut-audit signal.
2. Keep BNS upper bounds `UNVERIFIED` for all rows; extend gemgroup-based sensitivity only with batch-like labels, never as replicate-derived performance.
3. Run a GPU or prediction-only GEARS replication if any exact GEARS performance claim is needed outside the pilot.
4. Treat the failed L2 metadata folder (`results/pilot/gears_20260822T120129Z/`) as documented failure provenance; the completed L2 run is `results/pilot/gears_20260822T122126Z/`.
5. Add pathway-level annotations later only if a redistributable source is available; MSigDB direct downloads require registration.
